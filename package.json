{
  "name": "pi-terse-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Lean, focused extensions for the Pi coding agent. A monorepo of independently publishable pi packages.",
  "license": "MIT",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm run test --workspaces --if-present"
  },
  "engines": { "node": ">=22.19.0" }
}